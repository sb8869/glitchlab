/**
 * Generated remediation copy, written by scripts/generate-remediation.ts.
 *
 * Committed on purpose. Generation happens offline so the deployed game needs
 * no API key, no backend and no network at play time — which is also why the
 * cost shown on screen is a real measured number rather than an estimate.
 *
 * Empty here means the app runs entirely on the deterministic fallback.
 */

import type { GenerationRecord } from "./types.ts";

export const GENERATED: GenerationRecord = {
  generatedAt: "",
  entries: {},
  costs: [],
  totalUsd: 0,
};
